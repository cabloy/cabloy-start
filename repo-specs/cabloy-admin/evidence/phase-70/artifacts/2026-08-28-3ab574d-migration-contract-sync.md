# Phase 70 Migration and Contract Synchronization

- **Tested candidate base revision:** `3ab574de6435acd59047f2bd5993df867fc92f9e`
- **Execution date:** 2026-08-28
- **Edition / flavor:** Cabloy Start / Start Admin (`cabloyStartAdmin`)
- **Traceability:** `WBS-ADM-70-01`; [ATP-ADM-CTR-01](../../phase-60/ATP-ADM-CTR-01.md); `SRS-ADM-API-04`, `SRS-ADM-API-05`
- **Result:** pass

## Migration decision

The Student version-1 migration was reviewed against repository history. An earlier version-1 form had an inline Student description field; the current version-1 form creates `trainingStudent` plus the relation-backed `trainingStudentContent` table. The owner confirmed that the former version-1 schema was never released or deployed.

The resulting confirmed strategy is an intentional unreleased initial-schema fold:

- retain `vonaModule.fileVersion: 1`;
- do not add a version-2 migration;
- do not change `meta.version.ts` for this decision;
- retain `trainingStudentContent` as the initial version-1 schema shape.

No changed `meta.version.ts` file exists in this candidate worktree. The repository regression command below nevertheless passed, so no unverified migration path is introduced.

## Forward contract fixed point

Vona metadata was regenerated for `admin-user`, `admin-rbac`, `admin-role`, `admin-department`, `training-student`, and `training-record`. Zova metadata was regenerated for `admin-user`, `admin-rbac`, `admin-role`, `admin-department`, and `training-student`; `training-record` is backend-only and was intentionally excluded from Zova generation.

A controlled candidate Vona runtime supplied the Swagger input for supported Start Admin consumers (`admin-user`, `admin-rbac`, `admin-role`, and `admin-department`):

- Swagger size: 612,771 bytes;
- Swagger SHA-256: `2bed8ae930c1f63cd99f5d35b08361ff83e2bd5e88c4fa7094729e6600097e1b`;
- repeated Swagger retrieval: byte-identical;
- repeated generated OpenAPI inventory: byte-identical;
- repeated Vona/Zova metadata inventory: byte-identical.

The first generation changed the eight selected generated `src/api/openapi/types.ts` and `schemas.ts` files. No generated API classes changed. The change is source-backed: Student contracts replace the former inline description with relation-backed `content.descriptionMarkdown`, exposing content-derived fields where applicable. This generator snapshots the full Swagger document into each selected consumer’s shared type/schema surfaces before its operation matcher filters API classes. The broad shared type/schema updates are therefore expected generator output, not consumer matcher leakage and not manual generated-file editing.

`training-record/src/.metadata/index.ts` was regenerated by the Vona metadata CLI and reached the same fixed point. Its controller path ordering reflects current controller metadata; it was not manually corrected.

## Paired Start Admin reverse handoff

The following required order completed successfully after regeneration:

```bash
npm run build:zova:admin
npm run deps:vona
npm run deps:zova
npm run tsc
git diff --check
```

The Start Admin build produced both SSR and REST artifacts before Vona dependency synchronization. Dependency handoffs and root typechecking passed. `git diff --check` passed.

## Boundary

This record establishes current-candidate migration/version reasoning and stable contract synchronization only. Quality-gate and release status are recorded separately in the Phase 70 index and companion artifacts.
